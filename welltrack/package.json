{
  "name": "welltrack",
  "private": true,
  "version": "0.1.0",
  "description": "Shared lint/format tooling for the WellTrack server and client packages",
  "scripts": {
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "npm run test --prefix server"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.70.0"
  }
}
